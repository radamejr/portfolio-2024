import "./nav-bar.scss";

const NavBar = () => {
  return (
    <div className="navbar-wrapper">
      <button>Home</button>
      <button>Technologies</button>
      <button>Work</button>
    </div>
  );
};

export default NavBar;
